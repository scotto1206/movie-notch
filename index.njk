---
layout: base.njk
title: Movie Notch
---

<div class="container">
  {% for post in collections.post | reverse %}
    <div class="post">
      <h2><a href="{{ post.url }}">{{ post.data.title }}</a></h2>
      <p class="date">{{ post.date | date("MMMM d, yyyy") }}</p>
      <p>{{ post.data.excerpt or "No preview available." }}</p>
    </div>
  {% endfor %}
</div>
